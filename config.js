// ============================================================
//  config.js — 只需要改这个文件！
// ============================================================

window.CARD_CONFIG = {
  // 你的 Google OAuth Client ID（在 Google Cloud Console 获取）
  googleClientId: "YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com",

  // 你的 Cloudflare Worker 网址（部署后填入）
  workerUrl: "https://farewell-mm.YOUR_SUBDOMAIN.workers.dev",

  // 允许留言的邮箱域名
  allowedDomain: "shopee.com",

  // 卡片标题信息
  recipientName: "MM",
  subtitle: "小舟从此逝，江海度余生",
};
